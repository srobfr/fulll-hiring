/**
 * This is the default user id for this app.
 * A real application should not have a static id for the main (admin) user.
 */
export const defaultUserId = "f832d504-5194-4500-acb5-aab6330b8969";

/**
 * This is the plate number of a vehicle which is already inserted at database bootstrap to ease tests.
 * A real application should have a vehicle creation feature instead.
 */
export const defaultVehiclePlateNumber = "AB-123-CD";
